spring:

  auth:
    token: 445bf8df03f7e885c90fcbd21c692d7d3531b9f6b7ac2d35c4d1c63b1dd746bf

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:study-security
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug